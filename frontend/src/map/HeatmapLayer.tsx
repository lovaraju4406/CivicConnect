/** HeatmapLayer — Stub. Requires leaflet.heat plugin + backend heatmap data. */
export default function HeatmapLayer(_props: { points?: [number,number,number][] }) {
  return null; // Will render heatmap overlay after backend integration
}