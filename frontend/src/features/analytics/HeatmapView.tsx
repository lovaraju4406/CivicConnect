/** HeatmapView — Stub. Will use backend heatmap data + Leaflet after integration. */
export default function HeatmapView() {
  return(
    <div style={{background:"#111827",border:"1px solid #1e293b",borderRadius:"14px",padding:"24px",textAlign:"center"}}>
      <div style={{fontSize:"32px",marginBottom:"10px"}}>🗺️</div>
      <div style={{fontSize:"14px",fontWeight:700,color:"#e2e8f0",marginBottom:"6px"}}>Heatmap View</div>
      <div style={{fontSize:"12px",color:"#475569"}}>Available after backend integration.<br/>Will show complaint density by district.</div>
    </div>
  );
}